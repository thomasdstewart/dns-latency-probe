# Pinned dependency set for reproducible local development and CI runs.
#
# Usage examples:
#   pip install -c constraints.txt -e .
#   pip install -c constraints.txt -e .[dev]
#
# Refresh this file intentionally when you want to upgrade dependencies.
black==24.10.0
matplotlib==3.9.2
mypy==1.13.0
pytest==8.3.4
pytest-cov==5.0.0
ruff==0.6.9
scapy==2.5.0
types-setuptools==75.3.0.20241107
